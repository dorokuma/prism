# prism

> Version: v0.27.4  Date: 2026-09-03  Status: living document

LLM API Load Balancer  
Multi-account round-robin, exhaustion / cooldown, Chat↔Responses translation.

## Quick start (local)

```bash
git clone <repo>
cd prism
go build -o prism ./cmd/prism
cp config.yaml.example config.yaml
# set keys (pick one):
#   export PRISM_KEY_ACCOUNT_1=... PRISM_KEY_ACCOUNT_2=...
#   or put key: in config.yaml (local only, never commit)
./prism
```

Default listen: `127.0.0.1:18790` if unset. Prefer `127.0.0.1:18790` in production.

## Production layout (eqi / systemd)

| Piece | Path |
|-------|------|
| Binary | `/usr/local/bin/prism` |
| Runtime config | `/var/lib/prism/config.yaml` |
| Tool cache (optional) | `/var/lib/prism/mcp_tools.json` |
| Account secrets | `/etc/credstore/prism/PRISM_KEY_*` via `LoadCredential` |
| Unit | `/etc/systemd/system/prism.service` (see `scripts/prism.service.example`) |

Source tree (`/root/prism` or the git clone) is for build only — **no production `config.yaml` / `.env`**.

### Account keys

For each account `name` (e.g. `go-plan-1`), if `key` is empty in YAML the process loads:

1. **systemd LoadCredential** file named `PRISM_KEY_GO_PLAN_1` under `$CREDENTIALS_DIRECTORY`
2. else environment **`PRISM_KEY_GO_PLAN_1`**

Hyphens in the account name become underscores in the credential/env name.

### Deploy / update binary

```bash
cd /path/to/prism
./deploy.sh   # HEALTH_URL / HEALTH_TIMEOUT are overridable; defaults below
```

`deploy.sh` builds, atomically replaces the binary, runs `systemctl restart prism`, health-checks, and rolls back automatically on failure.

- `HEALTH_URL`: health-check URL, default `http://127.0.0.1:18790/ready` (override for a non-default port or remote probe; older deploys can point it back at `/health`). The default is `/ready` (readiness: 200 only when at least one account is healthy and not in cooldown), not `/health` (liveness: 200 as soon as the process is up, which cannot detect an all-accounts-down deploy). `/ready` is a **fail-closed deploy gate**: when every account is in cooldown/exhausted (bad upstream key, upstream outage) the health check fails and the deploy rolls back — by design, so a build that corrupts account state must not ship. For liveness-only gating set `HEALTH_URL=http://127.0.0.1:18790/health` explicitly. If `HEALTH_URL` ends in `/ready` and the old process is already failing this round (not up, or the pool was already not ready), this round probes `/health` on the same host so an already-down upstream is not treated as a new-binary failure, rolled back, and restarted a second time.
- `HEALTH_TIMEOUT`: health wait window in seconds, default `35`, must be a positive integer (invalid values `exit 3` before any side effects; prism is untouched). After restart, poll `systemctl is-active` + `HEALTH_URL` every 1s; continue on the first success, roll back only when the window expires; the post-rollback check uses the same poll. The process listens first: `/health` is immediately 200, `/ready` stays fail-closed. The window waits for at least one successful account probe, not for Listen (per-account `ProbeTimeout` is 30s max). **Do not** stretch `HEALTH_TIMEOUT` to minutes hoping every account recovers — that only delays a rollback that is already determined; roll back and inspect (`journalctl -u prism`).
- `HEALTH_CURL_MAX_TIME`: per-probe `curl --max-time` in seconds, default `5`, must be a positive integer (invalid values also `exit 3` before side effects). The actual timeout is `min(HEALTH_CURL_MAX_TIME, HEALTH_TIMEOUT)` so one curl cannot outlast the whole window.

Manual deploy:

```bash
cd /path/to/prism
go build -o prism ./cmd/prism
install -m 755 prism /usr/local/bin/prism
# config edits: edit /var/lib/prism/config.yaml then:
systemctl restart prism   # only when you intend downtime / reload
# or: systemctl kill -s HUP prism   # if process supports SIGHUP for partial reload
```

### Tool config sync (`models.json`) ownership

`prism` rewrites pi's `~/.pi/agent/models.json` after every model refresh
(startup fill, 24h stale refresh, SIGHUP). The write is atomic (temp file +
rename) and preserves the deployed file's mode and owner/group via `chown`
on the temp file. Two consequences for installation:

- `models.json` should be owned by the prism service user (or its group,
  with group-write permissions), OR prism must be allowed to preserve the
  existing owner (the process needs root / `CAP_CHOWN` for that).
- If the owner cannot be preserved — e.g. `EPERM` under `NoNewPrivileges`
  with an empty `CapabilityBoundingSet` — the sync ABORTS with an error:
  it never renames a differently-owned file over the deployed one and
  never falls back to a non-atomic in-place overwrite. The old file stays
  byte-for-byte intact (same inode, owner, mode) and the failure is
  visible in the logs, so the ownership misconfiguration is surfaced
  instead of silently degrading.

`prism setup` normalizes an existing tool config at install time: owner →
the `prism` service user, group → `pi-sync` when that group exists
(otherwise kept), mode → at least `0664` (missing bits added, none
removed). A file that does not exist yet is left alone — the first sync
creates it as the prism process (temp file + rename), so its owner is the
service user by construction. Setup grants no capability anywhere (the
unit's `CapabilityBoundingSet` stays empty — `CAP_CHOWN` is deliberately
never granted; see `scripts/prism.service.example`). If the normalization
cannot be applied (e.g. the `prism` user is missing, or `chown` is
denied), setup fails with an explicit error.

## Configuration

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `listen` | string | `127.0.0.1:18790` | Listen address |
| `probe_interval` | duration | `10m` | Exhausted-account probe interval |
| `quota_revive_after` | duration | `30m` | Cooldown window before a quota-exhausted account may revive (`0` disables wait; quota-exhausted accounts with `probe_path: disabled` auto-revive after this window). Requires restart (ReloadConfig warns) |
| `model_cache_refresh_interval` | duration | `3h` | Model cache periodic refresh interval. `0` disables periodic refresh. Negative values rejected; `< 1m` warns and falls back to `3h`. Requires restart (ReloadConfig warns) |
| `model_cache_refresh_strategy` | string | `full` | Model cache periodic refresh strategy: `full` (refreshes all providers every round) or `stale` (refreshes only providers with expired/missing cache). Hot-reloadable (SIGHUP) |
| `wire_api` | string | `both` | `legacy` / `responses` / `both` |
| `accounts` | array | required | Upstream accounts |
| `model_tiers` | map | — | Tier → upstream model |
| `model_remap` | map | — | Virtual model → tier |
| `default_tier` | string | — | Fallback tier |
| `default_provider` | string | — | Fallback provider for requests missing the X-Prism-Provider header (chat/completions and GET /v1/models). Unset = chat rejects with HTTP 400; /v1/models returns an empty 200 list |
| `max_concurrent_per_account` | map | — | Model → max concurrent requests per account (exact match; silences the unknown-model default warning). The limit is PER MODEL (the client-requested model name is the concurrency key): each model owns an independent in-flight counter on the account, so two models are isolated even when they share the same max value (a max=2 model is never starved by another max=2 model's traffic) and a low-limit model is never starved by a high-limit one (a max=1 model still gets its slot while a max=10 model holds 10). Per-model values hot-reload; the model key is stable across reloads. Account-wide in-flight stays observable as the sum |
| `max_concurrent_per_account_total` | int | sum of distinct positive per-model values (or 8) | Per-account AGGREGATE concurrency cap across ALL models (applied to every account). Bounds the sum of the per-model counters so N configured models can never stack N×max in-flight on one account. `0`/absent → the backward-compatible default (the sum of the distinct positive `max_concurrent_per_account` values — exactly the old per-max-value-grouped worst case, so an unconfigured deployment sees the same total while models stay isolated; same-max models share the aggregate budget, never a counter); negative or above 2³¹−1 = load error. NOT hot-reloadable (restart required; ReloadConfig warns) |
| `trusted_proxies` | array | — | CIDR list whose forwarding hops are trusted for client-IP resolution (rate limiting). When the RemoteAddr is inside a trusted CIDR, `X-Forwarded-For` is walked right-to-left (first untrusted valid IP wins); `X-Real-IP` is accepted ONLY when XFF is absent AND the value is a valid IP outside the trusted CIDRs; an all-trusted XFF or a trusted/invalid X-Real-IP falls back to RemoteAddr. Untrusted RemoteAddrs ignore the forwarding headers entirely. Whole-address-space ranges (`0.0.0.0/0`, `::/0`) are rejected at load: with every address "trusted" the chain walk finds no untrusted client hop, so client-IP resolution collapses to RemoteAddr and per-client rate limiting silently degrades to keying on the proxy's address for all proxied traffic (`X-Real-IP` never rescues it — it is only accepted when untrusted). Authenticated requests (`api_keys` non-empty and a matching Bearer) use a per-key bucket `key:<name>` instead of the client IP, so a local reverse proxy that presents `127.0.0.1` does not collapse every client into one bucket; failed/missing tokens still limit by IP |
| `max_upstream_response_bytes` | int | `33554432` (32 MiB) | Cap for non-streaming upstream response bodies (both the legacy chat path and the responses translation path) AND for model-cache success reads (`/v1/models` and ollama `/api/show`); larger bodies are rejected with HTTP 502 `response_too_large` (chat) or the model fetch fails (`model_fetch_failed`). `0`/absent = default, negative or above `268435456` (256 MiB, hard upper bound) = startup error. Hot-reloadable |
| `usage` | map | disabled | Token usage recording to SQLite: `enabled` (default false, opt-in), `db_path` (default `/var/lib/prism/usage.db`; NOT hot-reloadable), `retention_days` (absent → default 30; explicit `0` → keep forever, cleanup disabled), `channel_size` (default 4096), `batch_size` (default 50), `batch_flush_ms` (default 200), `default_key_id` (default `anonymous`; key_id recorded for requests without an authenticated api key — explicit empty falls back to `anonymous`). Cost is priced from `model_metadata[].cost` (USD per 1M tokens); an optional `long_context` / `long_context_threshold` tier is selected using prompt/input tokens with an inclusive `>= threshold` comparison (completion tokens do not trigger it). The pricing basis follows the upstream wire format (OpenAI `prompt_tokens` includes cached tokens, Anthropic `input_tokens` excludes the cache counters) and is persisted per row as `usage_source`. Usage recording is BEST-EFFORT and NOT a strict billing source: the recorder is async and non-blocking — a full queue, recorder shutdown or store failure drops events (counted in the `/metrics` expvars `usage_events_dropped` / `usage_write_errors` and rate-limited error logs) without ever affecting request forwarding; treat reports as estimates, and reconcile against the audit log for exact figures |

### Model remapping behavior

`model_remap` maps virtual model names to tiers. `model_tiers` maps each tier to an
upstream model name. The resolution logic is:

1. If the requested model is **in `model_remap`**:
   - Look up its tier, then look up that tier in `model_tiers`.
   - If both mappings exist → use the upstream model.
   - If the tier has no upstream mapping → fall back to `default_tier` (if set).
2. If the requested model is **not in `model_remap`**: pass through unchanged.
   (Real upstream model names are sent directly to the provider.)

> **Note:** Previously, all unknown models went through `default_tier`. The current
> behavior only applies `default_tier` to models that exist in `model_remap` but
> whose target tier has no upstream mapping.
| `mcp_tools_json` | string | — | Optional path to tool-definitions JSON. Cached MCP tools are bounded per API-key identity (100 tools) AND per single tool (1 MiB serialized: an oversized tool is dropped, never truncated, never logged in full) |
| `model_metadata` | map | — | Per-model metadata returned by /v1/models: context_window, max_tokens, reasoning, input, cost, thinking_level_map, extra. `cost` unit is USD per 1M tokens (e.g. `input: 0.6` = $0.6 per 1M input tokens). An optional `long_context` tier with `long_context_threshold` applies to prompt/input tokens when `tokens >= threshold`; completion tokens do not affect tier selection. |

### `wire_api`

| Value | Path | Typical client |
|-------|------|----------------|
| `legacy` | `POST /v1/chat/completions` only | Legacy clients |
| `responses` | `POST /v1/responses` only | Codex CLI |
| `both` | Both | Mixed |

### Accounts

| Field | Description |
|-------|-------------|
| `name` | Label; also builds `PRISM_KEY_*` name. MUST be non-empty and unique, start with an ASCII letter/digit, and contain only ASCII letters/digits/`_`/`-` (max 64 bytes); two names that fold to the same `PRISM_KEY_*` (e.g. `a-b` vs `a_b`) are rejected at load |
| `key` | Optional inline key (avoid in production) |
| `base_url` | Upstream API base |
| `provider` | REQUIRED since v0.17.0: every account must declare a provider (via `account.provider` or a `providers:` block). A provider-less account can never be selected by business requests (they route by provider) and loading now fails with an explicit error — **compat note**: configs with bare top-level `accounts:` entries must add `provider:` or move them under a `providers:` block |
| `public_service` | Optional bool (default false). When true (or set on the provider block), HTTP 402 and structured PermanentQuota errors from upstream do not mark the account exhausted or lock it out with QuotaReviveAfter |

## Endpoints

| Path | Method | Description |
|------|--------|-------------|
| `/v1/chat/completions` | POST | Chat proxy (non-POST → 405 `method_not_allowed`) |
| `/v1/responses` | POST | Responses path (non-POST → 405 `method_not_allowed`) |
| `/v1/models` | GET | Model list. No `X-Prism-Provider` header uses `default_provider` when set (same fallback as chat); if that is also unset → empty 200 list. Cache hit → cached models. Cache miss → fetch from upstream; fetch failure is never a 200 empty list: no healthy account → 503 `no_healthy`, account saturated → 503 `model_fetch_saturated`, other upstream/parse/size errors → 502 `model_fetch_failed`. Request body cap 10 MiB (413 `request_too_large` / 400 `invalid_request`) on the POST surfaces |
| `/prism/v1/models/refresh` | POST, GET | Administrative model cache refresh (POST) and read-only status query (GET). Accepts optional query param `?provider=<name>` to refresh or query a single provider. POST returns HTTP 202 `accepted` with JSON status of scheduled providers; GET returns HTTP 200 `ok` with current provider snapshot without triggering a refresh. Protected by admin auth (`PRISM_ADMIN_TOKEN` / loopback) before global `api_keys` gate, with dedicated rate limiter for POST (1 req / 10s, burst 2). CLI: `prism models refresh` / `prism models status` |
| `/health` | GET | Liveness: `ok` whenever the process is serving. Bypasses the api_keys gate and the rate limiter |
| `/ready` | GET | Readiness: `ok` only when at least one account is healthy AND out of cooldown, otherwise 503 `not ready`. Bypasses the api_keys gate and the rate limiter (like `/health`); used by `deploy.sh` / load balancers. **Fail-closed**: when EVERY account is exhausted or in cooldown (bad key rotation, upstream outage) the deploy health check fails and `deploy.sh` rolls back — by design, so a version that breaks account state never ships. If the pre-restart `/ready` probe already fails (old process down or pool already not ready), `deploy.sh` switches this round to the same host's `/health` so an already-unready pool is not treated as a new-binary failure and rolled back with a second restart. Deployments that want liveness-only gating set `HEALTH_URL=http://127.0.0.1:18790/health` explicitly; do not paper over an all-accounts-down state by extending `HEALTH_TIMEOUT` to minutes |
| `/metrics` | GET | expvar metrics. Auth (fail-closed): when `METRICS_TOKEN` is configured every request — loopback included — must present it as a Bearer token; only when it is unset is a direct loopback request (no `X-Forwarded-For`/`X-Real-IP`/`Forwarded`) allowed without a token, while loopback with forwarding headers (same-machine reverse proxy) and all remote requests are denied |
| `/admin/usage/summary` | GET | Aggregated token usage/cost summary (when `usage.enabled`). Auth (fail-closed): when `PRISM_ADMIN_TOKEN` is configured every request — loopback included — must present it as a Bearer token (the token is re-read from the environment on every request, so rotation takes effect without a restart); only when it is unset is a direct loopback request (no `X-Forwarded-For`/`X-Real-IP`/`Forwarded`) allowed; mounted before the global api_keys gate (like `/metrics`). Query params: `from`/`to` (unix seconds; omitted `from` defaults to the SuperGrok week start — a `to`-only request pins only the upper bound and the lower bound still falls back to the week start, matching the CLI `--until`-only window — omitted `to` is now; explicit `from=0` keeps all-time; explicit negative `from`/`to` return 400 `invalid from` / `invalid to`), `group_by` (`model`,`provider`,`account`,`key_id`,`stream`,`success`,`hour`,`day`), filters `model`/`provider`/`account`/`key_id`/`stream`/`success`, `limit` (default 100, max 1000). `format=table` renders the same text report as the CLI: with neither `from` nor `to` the header aggregates ALL history while the detail rows keep the default SuperGrok week window (same as bare `prism usage`; the header label is `总请求`) — user-visible labels use "tokens" (input tokens / output tokens) — and always uses the single-line compact table suited to the Pi panel: one row per model/group, short headers (model / reqs / in tokens / cache / hit% / out tokens / cost, plus unpriced when unpriced rows exist), a one-space column gap with the same two-space left indent as the summary and warning lines, requests and token totals in compact k/M notation, while unpriced counts remain exact, compact cost, and model/group names shown in full — never truncated (numbers are never truncated either); it never depends on the terminal width and has no other layout. `format=json` is unaffected. Returns 503 `store_unavailable` when usage is disabled or the store failed |
| `/admin/quota` | GET | SuperGrok weekly pool (`creditUsagePercent` + reset) and Gemini 5h/weekly (`retrieveUserQuotaSummary`, Claude/GPT buckets dropped). Independent of `usage.enabled`. Same `PRISM_ADMIN_TOKEN` auth as `/admin/usage/summary`. `format=table` for the CLI-style text report. Returns 503 `quota_unavailable` when `quota.enabled` is false. CLI: `prism quota` |


### Codex

`~/.codex/config.toml` example:

```toml
[model_providers.prism]
name = "prism"
base_url = "http://127.0.0.1:18790/v1"
requires_openai_auth = false
api_key = "lb-local-placeholder"
wire_api = "responses"

model_provider = "prism"
model = "gpt-5.5"
```

### Tool definitions cache

Optional JSON used when injecting tool schemas for some clients. Generate from Codex MCP config:

```bash
python3 scripts/generate_mcp_tools.py /var/lib/prism/mcp_tools.json
systemctl kill -s HUP prism   # or restart
```

### Model Cache CLI

Manage and query upstream model caches directly or via HTTP:

```bash
# Query cache status via HTTP GET (read-only, does not trigger refresh)
prism models status

# Refresh all providers via HTTP API (default)
prism models refresh

# Refresh specific provider
prism models refresh --provider opencode-go

# Refresh in direct mode (writes to /var/lib/prism/model_cache directly, does NOT notify running prism instances; use ONLY when service is not running)
prism models refresh --direct --config /var/lib/prism/config.yaml

# Output machine-readable JSON
prism models refresh --json
```

#### Rollback Preset

To restore the legacy behavior (refresh on-demand or after 24h expiration):

```yaml
model_cache_refresh_strategy: stale
model_cache_refresh_interval: 24h
```

## License

MIT

## Changelog

- **2026-09-05** — v0.27.6 — fix(planusage): poll Gemini quota from daily-cloudcode-pa pool. Gemini 配额轮询端点从 cloudcode-pa.googleapis.com 切到 daily-cloudcode-pa.googleapis.com（AGY 实际消耗池），旧端点 5h 恒 0%；GeminiFetcher.Match 补齐 daily-cloudcode-pa host 匹配。
- **2026-09-04** — v0.27.5 — feat(pool): configurable quota_revive_after (default 30m) and probe-disabled quota auto-revive. Quota 耗尽恢复窗口由硬编码 6h 改为可配置项 `quota_revive_after`（默认 30m，支持标准 duration 格式；配 0/0s 禁用窗口等待），启用探活的账号在窗口期满后探活成功即可回池；`probe_path: disabled` 的 quota 耗尽账号在窗口期满后自动回池自愈（依赖请求侧错误处理兜底）；`probe_path: disabled` 的凭证失效（credential，如 401）账号不自动自愈；启用探活的 credential 账号探活 200 即回池（既有行为，与本次改动无关）。修复 disabled 凭证需人工介入并重启生效——热重载不重置池内账号状态。
- **2026-09-03** — v0.27.4 — fix(usage): negative explicit from/to rejected, walkable-dir probe (EvalSymlinks+ReadDir), walk abort on tree permission errors, CLI duration overflow cap (h/m/s and d), in-tree non-regular skip, and pi-sessions probe wiring. `/admin/usage/summary` 显式传入的 `from`/`to` 为负数时返回 400 `invalid from` / `invalid to`（此前负数会静默退化为无界窗口，掩盖错误输入）；`from=0` 显式全历史与 `to=0` 无上界的既有语义不变。CLI 侧 `prism usage` 的 `--since`/`--until` 解析结果早于 Unix 纪元（如 `0001-01-01`；相对写法 `30m/24h/7d` 正常输入不会产生负 ts；超大天数如 `999999999d` 由 d 上限拦下报「过大」）时同样报错，不再静默按无界处理；相对时长 h/m/s 在 `n*unit` 乘法换算前按 `n > math.MaxInt64/int64(unit)` 判定溢出（约 292 年，如 `5124094h`），d 单位按日历算术另设上限 `n > 3652500`（约一万年；不可套 MaxInt64/unit，AddDate 会 wrap 落入过去窗口），一律报错 `时间跨度 %q 过大`。sessions 目录可导入性探测抽成包内 `probeWalkableDir`（Stat → EvalSymlinks → ReadDir），`ImportGrokBuild` 与 `ImportPiSessions` 共用，Walk 走 EvalSymlinks 真实路径：合法 symlink→dir 解析后正常导入。根层形态由 probe 挡：目录不存在、不是目录（sessionsDir 根路径的 FIFO/普通文件/symlink→文件由 Stat IsDir 挡住）、坏链（Stat 或 EvalSymlinks 失败）、或根层不可枚举（--x 直接 open 失败；ReadDir 其他错误）则跳过整轮导入含窗口 DELETE。树深处权限错误（如 r-- 根过 probe 后 Walk 逐条 lstat 需 x 得 EACCES）由 walkFn 中止导入、不删行——不确定收成完整性就不删旧数据；树内非 regular 文件（FIFO/socket/device）由 walkFn 跳过不 Open，避免挂死 poller。pi-sessions 同样：树内权限错误中止导入不删行。已知限制：filepath.Walk 不跟随树内 symlink→dir，只经树内 symlink 可达的会话数据不会被导入。跳过/中止各记 slog.Warn（含来源 grok-build / pi-sessions、路径与原因）。服务侧 quota poller 不再自动导入 grok 会话（仅 `prism quota` CLI 手动导入）。树内文件 Open 权限不足时记 slog.Warn（含 path 与 error）并跳过该文件。测试：`?from=-1` / `?to=-100` 400、`from=0` / `to=0` 仍 200、sessionsDir 指向普通文件/symlink→文件/坏链跳过且预插行不删、symlink→dir 导入真实数据、walk 中止不删行、树内 FIFO 跳过不挂死、`parseTimeArg` 纪元前与过大跨度（含 d 上限）报错、pi-sessions 目录不存在不删行。
- **2026-09-03** — v0.27.3 — fix(usage): inverted-range 400, empty-value presence, and grok-build import skip. `/admin/usage/summary` 在窗口最终确定后（applyDefaultRange 补齐默认下界之后）校验 from>to：显式倒挂与 to-only 且 `to` 早于 grok 周起点（窗口 `[周起点, to]` 空且倒挂）一律 400 `invalid range: from after to`，与 CLI `--since 晚于 --until` 报错语义对齐；`?from=` / `?to=` 空值视为未传（presence 判定与 parseSummaryQuery 的空值跳过、CLI `--since ""` / `--until ""` 一致），不再误入 to-only 分支。`ImportGrokBuild` 开头对 sessions 目录做可读性探测（Open 而非 Stat：Stat 对缺读权限的目录仍成功），目录不存在或不可读时整体跳过——包括窗口内 DELETE，断掉服务侧 poller 在不可读 `/root/.grok/sessions` 下的“先删后插零”空转（每轮轮询清空已导入的 grok-build 行再插零条）；预检是运行时权限判断，`prism quota` CLI 以 root 运行可读目录，导入不受影响。测试：to 上界钉死（`to=now` 之后的事件不进 rows 与表头计数）、`to=abc` 非数字 400、from>to / to-only 早于周起点 400、空值 presence、不可读目录不删行。
- **2026-09-02** — v0.27.1 — fix(oauth): read google client secret via credentials directory. clientSecret 解析顺序对齐 config.getCredential：显式 Config 值 → systemd LoadCredential 目录（$CREDENTIALS_DIRECTORY 下按文件名读取——该主机注入的 secret 以文件形式暴露、不走环境变量）→ 环境变量，修复 Gemini token 刷新因取不到注入 secret 而报 client_secret is missing 的问题。
- **2026-09-03** — v0.27.2 — fix(usage): no auto-import, all-history default header, and disabled-config skip. `prism usage` no longer auto-imports Grok Build / Pi session files into the usage database before every run — the ledger is exactly what the running service recorded, no side-effect writes from a report command. The default report (no time params on `/admin/usage/summary?format=table`, bare `prism usage`) now shows ALL history in the header aggregate while the detail rows keep the SuperGrok week window, and the header label is renamed from `请求` to `总请求` (shared `render.Summary` line). `resolveUsageDBPath` skips a config candidate whose `usage.enabled` is false instead of letting its default-filled `db_path` shadow a later candidate's real database. `/admin/usage/summary` 只传 `to` 不传 `from` 时，rows 窗口从 `[0, to]` 收窄为 `[grok 周起点, to]`，与 CLI `--until` 单传语义对齐。
- **2026-09-02** — v0.27.0 — feat: Gemini/Antigravity plan quota + OAuth google + quota table revamp. New `oauth: google` provider (`prism auth google` imports the Antigravity CLI token from ~/.gemini/antigravity-cli and refreshes it on the request path; token files under oauth_dir, invalid_grant terminal latch, 2h keepalive). Quota-only account pattern for Cloud Code: new `skip_model_cache` flag — a direct model fetch returns ErrModelCacheSkipped (502 model_fetch_failed on /v1/models, never a 200 empty list) while background refresh rounds skip silently without backoff — plus probe_path disabled and skip_pi_sync. `GeminiFetcher` polls `retrieveUserQuotaSummary`, keeps only the Gemini 5h/weekly buckets (Claude/GPT groups dropped), weekly PeriodStart inferred as ResetsAt-7d for the estimate. Week estimates switched from last week's frozen value to LIVE reversal (period consumed tokens ÷ period used percent × 100, recomputed every poll): SuperGrok estimate now tracks real usage. `/admin/quota` table is module layout — account name on the module's FIRST window row only, modules sorted by account so windows never look like they belong to a neighbour — window labels 短期(5h)/中期(weekly)/长期(monthly), all columns left-aligned, weekly no-data renders `--` while windows with no estimate concept render `-`. `WeekStartUnix` pins the default usage range to the xai weekly window (a Gemini weekly snapshot must never win the anchor — the /admin/usage/summary default range was flipping between providers). `SumTokensLike` gained a provider filter so third-party gemini-* rows (e.g. anyrouter gateways) never pollute the Antigravity estimate; the CLI `prism quota` gemini branch opens the usage DB read-only with an existence check (no empty-db side effect). Account names renamed to PascalCase (SuperGrok/Gemini) with usage history migrated (UPDATE usage_events). Gemini estimate stays `--` until a consumption source records gemini-* rows (Antigravity usage does not pass through prism); Google exposes only remaining fractions, so the reversal needs the local ledger. Double-reviewed (reviewer audit + oracle second opinion, two rounds).
- **2026-09-01** — v0.26.1 — fix(models): left-align the updated-at column in the models snapshot table to match its header.
- **2026-09-01** — v0.26.0 — feat(models): models snapshot table revamp and agentrouter provider rename. Switched models snapshot table to compact Chinese 3-column render.Table (供应商/模型/更新时间), removed BACKOFF column, shortened time format to MM-DD HH:MM, translated refresh/error status messages to Chinese, and renamed agentrouter-openai to agentrouter with anthropic protocol example cleaned up.
- **2026-09-01** — v0.25.0 — feat(usage): usage report revamp. Formatted model names (strip provider prefixes, date/-latest suffixes, truncate to max 20 chars), simplified top-level summary to one line per metric (requests, total tokens, total cost) and removed hit rate lines, dropped input/output token and cost columns from detail tables, and removed period header lines.
- **2026-08-31** — v0.24.0 — feat: 3h periodic full model cache refresh, administrative manual refresh HTTP API (`POST /prism/v1/models/refresh`), thin CLI (`prism models refresh`), and dual-review residue cleanup. Added configurable `model_cache_refresh_interval` (default 3h, 0 to disable) and `model_cache_refresh_strategy` (default "full", optional "stale"). Includes +/-10% periodic jitter, 0-2s provider stagger, failure exponential backoff (30s*2^(n-1) capped at 1h), deduped unique provider rounds, singleflight leader sharing, pending queue upgrades, dedicated 10s/req token-bucket rate limiting with admin auth, and `--direct` local CLI execution.
- **2026-08-30** — v0.23.1 — fix(usage): remove unpriced warning and unpriced column to prevent misleading cost totals.

- **2026-08-30** — v0.23.0 — feat: provider- and account-level `public_service` mode for zero-balance/public-service upstreams (HTTP 402 and structured PermanentQuota bypass pool exhaustion and QuotaReviveAfter lockouts; 401 and true 429 behaviors unchanged).
- **2026-08-30** — v0.22.7 — feat: sort usage summary by cache hit rate (hitRateSumExpr descending with request count as tie breaker)
- **2026-08-28** — v0.22.6 — fix: xAI OAuth token persistence — background keepalive refresh (2h), cross-process flock with disk re-read (CLI/service single-use token race), reactive 401 force-refresh retry, terminal invalid_grant circuit breaker with restart-free re-login recovery; 9 new race-tested cases
- **2026-08-28** — v0.22.5 — fix: TransformRequestBodyForProvider message role normalization (`role:developer` → `role:system`) now applies globally to all upstreams instead of being gated to ollama-only, preventing SYSTEM.md/AGENTS.md context loss or errors on upstreams that do not accept the developer role.
- **2026-08-28** — v0.22.4 — feat: credential prefix LB_KEY_ renamed to PRISM_KEY_ across configuration, CLI setup, systemd credentials, and documentation.
- **2026-08-24** — v0.22.3 — fix: pi-session hit-rate denominators now include Anthropic cache-read and cache-creation input, preventing hit rates above 100%; Pi rows are included in the Anthropic overview bucket.
- **2026-08-24** — v0.22.2 — feat: usage statistics now ingest Pi session data and merge all three sources within the same time window.
- **2026-08-24** — v0.22.1 — fix: usage summary layout. The `prism usage` and `/admin/usage/summary?format=table` summary block drops the failure and streaming segments (the second line now shows only the cache-hit segments), renames `缓存命中` to `命中`, upper-cases the source labels to `OpenAI`/`Anthropic`, and renames the SuperGrok week default header from `周限` to `本周` (shared table header and JSON `period` field via the `PeriodWeek` constant); `render.Summary` no longer carries failure/streaming counts. Tests updated.
- **2026-08-23** — v0.22.0 — feat: Pi model family sync and long-context model pricing. Pi `models.json` sync filters entries by provider family (`-anthropic` / `-openai` suffix): Anthropic providers keep only Claude/Anthropic ids and rewrite to `anthropic-messages` on the prism origin with the `/v1` suffix stripped (Pi posts `/v1/messages`); OpenAI-family providers exclude Anthropic ids. `model_metadata[].cost` gains an optional `long_context` tier with `long_context_threshold`: requests whose prompt/input tokens are `>= threshold` price all token categories at the long-context rate (completion tokens do not trigger it); a nested `long_context` is rejected at load with an explicit error.
- **2026-08-23** — v0.21.6 — fix: align Grok Build zero-rate guards with `priceFor` long-context tier selection, so unpriced tiers remain missing prices instead of recording zero-cost usage; tests cover short/long lookup and explicit cost precedence.
- **2026-08-23** — v0.21.5 — fix: Grok quota exhaustion. HTTP 402 and 429 responses with explicit quota-exhaustion markers now report the weekly window as 100% rate-limited without a query error; ordinary 429 responses and authentication/upstream failures still surface as errors.

- **2026-08-23** — v0.21.4 — feat: SuperGrok OAuth, weekly quota, and Pi `/usage` defaulting to the SuperGrok week window. `prism auth xai` runs the public Grok-CLI device flow; CLI config loads `./config.yaml` then `/var/lib/prism/config.yaml`; the token file is chowned like `oauth_dir` so `User=prism` can hot-load it. Quota polls only `cli-chat-proxy.grok.com/v1/billing?format=credits` (OpenCode Go `/usage` windows are gone); the window label is week-limit; the limit-estimate column is grok-* tokens × 100 / week-pool percent and shows the previous week after reset. Grok Build `updates.jsonl` session usage merges into the same week ledger (`key_id=grok-build`). xAI models get official short-context USD/1M prices and thinking maps. `/admin/usage/summary` and `prism usage` omit-`from` default to SuperGrok `PeriodStart` (else the estimate file, else last 7 days); explicit `from=0` / `--since` still override.

- **2026-08-22** — v0.21.3 — fix: usage detail table cache hit rate. Per-row hit rate used `cached/prompt_tokens`, which explodes past 100% for Anthropic (input_tokens excludes cache_read and cache_creation; a claude-opus-5 row of 490 input / 24.17M cache rendered millions of percent). Summary now aggregates a source-aware denominator as `hit_rate_input_tokens` (OpenAI-form prompt; Anthropic prompt+cached+cache_write; mixed groups sum both), and the table uses that — same split as the overview cache-hit (openai)/(anthropic) segments. Stored input-tokens/cache columns stay as-is; later price changes still do not rewrite history.

- **2026-08-22** — v0.21.2 — fix: AgentRouter Anthropic `/v1/messages` reports remaining credit below one request as HTTP 403 `new_api_error` with message `pre-consume quota failed, user quota: ＄X, need quota: ＄Y` and **no** `error.code`. v0.21.1 only matched `error.code` `insufficient_user_quota`, so that envelope stayed `upstream_4xx` and the account stayed in rotation. The classifier now treats structured `error.message` containing `pre-consume quota failed` as permanent quota exhaustion (account kicked, request fails over); unrelated `new_api_error` codes and plain-text quota strings are still temporary.

- **2026-08-21** — v0.21.0 — feat: setup ships `clinepass` as a fourth builtin (menu numbers and custom derived from the slice). Vendor-prefixed model ids are peeled for reasoning maps from the account `base_url` host (`cline.bot` and subdomains), not the provider name. `dsml_guard` / `skip_pi_sync` / `probe_path` stay runtime YAML. Chat and `/v1/responses` translation both apply `dsml_guard` (stream and non-stream); `/v1/messages` is unchanged. LoadConfig rejects mixing top-level `accounts:` with a `providers:` block (that mix used to drop the top-level list without error).

- **2026-08-17** — v0.20.9 — fix: usage reporting and cache pricing. `ParseOpenAI` fills a missing/zero `total_tokens` with prompt+completion (same fallback as Anthropic). Overview/Summary tokens use stored total when > 0, otherwise prompt+completion, so legacy rows that stored total=0 no longer show 0 tokens. `ComputeCost` treats `cache_read` 0 (YAML omitted) as unset and prices the cached portion at `input`, not free.

- **2026-08-17** — v0.20.8 — fix: three follow-ups from the issue-only audit. `GET /v1/models` without `X-Prism-Provider` now uses `default_provider` when configured (same fallback as chat; still an empty 200 list when both are unset). SIGHUP that flips `usage.enabled` keeps the running flag in the holder so `stream_options.include_usage` cannot diverge from the startup recorder. `prism quota` no longer uses `http.DefaultClient` (cross-host 3xx would forward a custom `auth_header`); the CLI fetch client shares `util.CheckSameHostRedirect` with the account HTTP client.

- **2026-08-16** — v0.20.7 — fix: `prism quota` and `/admin/quota?format=table` render each account name once per window group, vertically centered: on the weekly window row whenever a weekly window exists (even when that is not the group's middle row), otherwise on the middle row of the actual window count (the lower middle for even counts). The short/long rows of a three-window group leave the account cell empty; the provider prefix and the stale marker ride the single account cell, and error lines keep their full `provider/account` attribution. Column layout, indentation and JSON are unchanged.

- **2026-08-16** — v0.20.6 — fix: compact usage/quota tables and token wording. `prism usage` and `/admin/usage/summary?format=table` always render the same single-line compact table (no vertical card layout, no terminal-width probe): one row per model/group, short headers (model / reqs / in tokens / cache / hit% / out tokens / cost, plus unpriced when present), a one-space column gap with the same two-space left indent as the summary and warning lines, request and token cells in the existing k/M compact notation (unpriced counts remain exact), and the cost cell in a compact format (at most two decimals, tiny amounts keep three so a small fee never shows as $0; nil stays `-`) — display precision only, the stored aggregates are unchanged. Model/group names render in full and are never truncated; numbers never truncate either. The `COLUMNS`/`TIOCGWINSZ` width probe, the `format=table` `layout`/`width` params and the vertical card renderer are removed. User-visible token wording is unified: the table headers are in tokens / out tokens and the summary line's `token` is now tokens; JSON field names (`prompt_tokens`/`completion_tokens` etc.) are unchanged. `prism quota` and `/admin/quota?format=table` switch from per-account phone cards to the same compact single-line table rules (two-space indent, one-space column gap, no terminal-width dependency): columns account / window / status / used / reset / limit estimate, one row per window with the account repeated, names always in full; error lines now carry account attribution (`provider/account: fetch_failed`), multi-provider reports prefix the provider in the account column, and the rate-limited marker stays rate-limited. The limit-estimate column shows the upstream's documented window limit as `$N.00` only when the snapshot marks it estimated (never a real spend figure); the previous estimated suffix is dropped, and confirmed/unset windows render `-`. The old display-width/pad helpers are removed in favor of the shared `render` package.

- **2026-08-15** — v0.20.5 — fix: usage table display rework. The detail table header switches to labels (model / requests / input tokens / cache hits / hit rate / output tokens / cost), the model group column is titled model, the redundant Total column is dropped, and a cache hit-rate column is added (cached/prompt as a one-decimal percent; a zero prompt total renders a stable `0.0%` instead of NaN/Inf from a division by zero). Tests updated to pin the new header and cell layout.

- **2026-08-14** — v0.20.4 — fix: harden proxy responses and deployment. A `Content-Encoding: gzip` upstream body is now transparently decompressed on EVERY response path — non-streaming chat, non-streaming Responses translation, legacy chat SSE passthrough, Responses SSE translation, and the 4xx redaction/passthrough — so the client receives DECODED content while the gzip header is never copied (not in the response-header allowlist): body and headers stay semantically consistent instead of compressed bytes labeled `application/json` with no `Content-Encoding`. An upstream that CLAIMS gzip but sends a non-gzip body fails closed as a structured 502 before any byte is committed (the streaming paths validate the gzip header while the status is still uncommitted, delayed until the first event); a truncated gzip stream classifies as `upstream_error`, never `upstream_refused` — the upstream already answered, a refusal would be a lie. The body-reader close lifecycle is pinned: `resp.Body` is closed exactly once by the caller's top-level defer, and a gzip reader is a separate object closed on success AND on read error — no decompressor leak, no double close (close-count regression tests). The rate limiter no longer hard-rejects a NEW IP once the bucket cap (`config.RateLimitMaxBuckets`) is reached: new IPs are served from a single shared overflow bucket, so a flood of distinct IPs drains the shared tokens and is rate-limited like everyone else instead of being rejected for the whole idle TTL; the cleanup loop removes idle per-IP buckets and resets an idle overflow bucket (`SetCleanupForTest` makes it testable). Connection-error classification is now STRUCTURAL (`errors.Is`/`errors.As` over the real chain: `context.DeadlineExceeded`/`Canceled`, `syscall.ECONNREFUSED`/`ECONNRESET`/`ENETUNREACH`/`EHOSTUNREACH`, `*net.DNSError`) instead of substring matching — a business/parse error whose message merely contains "EOF" or "connection reset" is no longer misclassified as an upstream failure; the category strings and the `ConnErrorRetryable` gate (dial refused/DNS/unreachable/no route = retryable) are unchanged. The client-supplied `X-Prism-Provider` routing header is dropped by `copyClientHeaders` and never reaches the upstream (prism's own internally-built model-cache fetches still set it explicitly). `ensureStreamOptionsIncludeUsage` rewrites the body via `rebuildBodyWithStreamOptions`, preserving the top-level key ORDER and every other value's original bytes — the old whole-body re-marshal scrambled the key order nondeterministically; a missing `stream_options` is appended last and any structural surprise falls back to the input unchanged. `writeUpstreamExhausted` gains a defensive default branch answering the generic 503 `all_exhausted` (with audit fields) instead of a silent empty response. Deployment: `deploy.sh` and `scripts/install.sh` no longer swallow `systemctl restart prism` failures with `|| true` — a failed new-binary restart exits 3 (binary installed, backup kept at `$BACKUP` for manual rollback/diagnosis) and a failed rollback restart exits 2 (service may be down, manual intervention). Test hardening: new `scripts/test_deploy.sh` statically asserts every restart in both scripts has an explicit failure branch; `scripts/test_install.sh` adds injected-restart-failure scenarios (exit 3 with the backup kept; rollback-restart failure exits 2 with the old binary restored); Go tests cover gzip success/4xx/truncated/invalid fail-closed on every path, close-once body lifecycle, structural error classification, key-order preservation, `X-Prism-Provider` non-forwarding and the `all_exhausted` fallback.

- **2026-08-14** — v0.20.3 — fix: non-idempotent POST no longer switches accounts on HTTP 5xx/1xx/3xx (same rule as `ConnErrorRetryable` for timeout/reset/EOF). The failing account still cools down; the client gets 502 `upstream_5xx` with a fixed message, not a second POST and not `all_exhausted` 503. An uncommitted empty upstream SSE (legacy and Responses) also writes a terminal 502 `upstream_stream_error` and does not fail over. `scripts/install.sh` now backups the previous binary, probes `/ready` (falling back to `/health` when the old process is already unready), and rolls back on a failed health check; `scripts/test_install.sh` covers the success, rollback, and invalid-timeout paths without touching a live unit.

- **2026-08-13** — v0.20.2 — fix: eight audit follow-ups. Quota-exhausted accounts record `exhaustedAt`; a probe HTTP 200 revives them only after `QuotaReviveAfter` (default 6h at the time; superseded in v0.27.5 — configurable via `quota_revive_after`, default 30m; tests may set 0). SIGHUP TLS path changes warn and keep the running `tls_cert_file`/`tls_key_file` in the holder so SyncTools scheme matches the live listener. `deploy.sh` curl probes take `--max-time` (`HEALTH_CURL_MAX_TIME`, default 5, positive integer, actual timeout `min(HEALTH_CURL_MAX_TIME, HEALTH_TIMEOUT)`); a pre-restart `/ready` failure switches this round to `/health` to avoid a false rollback plus a second restart. Responses `item_reference` is rejected (same style as `previous_response_id`), not dropped. Non-idempotent POST no longer switches accounts on timeout/reset/EOF/broken pipe (`ConnErrorRetryable`); refused/DNS still fail over; a non-retryable transport error writes 502 instead of an empty body. An uncommitted empty upstream SSE cools the account and retries another; a whole loop of empty streams ends as 502 `upstream_stream_error`, not 503 `all_exhausted`. Authenticated `api_keys` rate-limit per `key:<name>` (live `holder.Load()`); missing/bad tokens stay on the IP bucket; `/health` and `/ready` remain exempt. A 499 client-disconnect audit stores a fixed `client disconnected` string, never the raw `*url.Error`.

- **2026-08-13** — v0.20.1 — fix: harden response content and stream arguments. Responses-to-Chat conversion now rejects image content in BOTH message-content shapes — the array form (`content: [{type: image_url|input_image|input_file}, ...]`) and the single-object form (`content: {type: image_url|input_image|input_file}`) — instead of silently converting them to text through the normalize path; single-object text content (`input_text`/`output_text`) is not misclassified (regression test: table-driven `TestResponsesToChat_ImageContentRejected`). The streaming Responses translation accumulates tool-call argument deltas in a `strings.Builder` (like the text/reasoning buffers) instead of repeated string re-concatenation, so thousands of small SSE fragments accumulate in O(n) overall and the final `function_call` `output_item.done` carries the COMPLETE concatenation, never truncated (regression test: `TestTranslateStream_ToolArgsManyFragments`, 2000 fragments / 8 KiB).

- **2026-08-13** — v0.20.0 — fix: audit hardening. SIGHUP reload validates `api_keys` against the running listen address (a public bind cannot drop keys by rewriting listen to loopback) and keeps the in-process listen value so `SyncTools` cannot publish a port the process is not bound to. Client `Api-Key` / `X-Goog-Api-Key` are stripped like other credential headers; the upstream credential is always the selected account key via `ApplyAuthHeader`. A 403 with a recognized permanent credential/quota envelope fails over like 401; a bare 403 still passes through. Mixed cooldown + full slots wake on the nearest cooldown instead of waiting out select timeout. Runtime `MarkExhaustedWithClass` records quota vs credential; a `/v1/models` 200 no longer revives quota-exhausted accounts; probe classification matches runtime and is capped at 10 concurrent probes. Empty Responses streams with `finish_reason`/`[DONE]` are legal completions. Reasoning `summary` arrays round-trip. MCP same-name tools replace the cached snapshot. `models.json` parse errors abort instead of wiping non-Prism providers; existing `apiKey` values are preserved; TLS writes `https://`; ollama `/api/show` uses a short separate budget and still persists `/v1/models` on show timeout. HTTP listen no longer waits for the startup probe wave; `TimeoutStopSec` is 45s. Plan-quota fetch uses the account auth header; unauthorized polls clear windows; debug dumps refuse symlink clobber; `WriteJSON` marshals before writing the status.

- **2026-08-13** — v0.19.0 — fix: legacy chat SSE passthrough no longer commits an empty 200 when the upstream stream fails or closes before the first event — the HTTP status is delayed until the first event write, so a pre-first-event read failure or an empty upstream stream answers a structured 502 `upstream_stream_error` (the audit records the actually-delivered status and error type; a healthy stream still passes through byte-for-byte with the upstream status and working Flush). Debug dumps are privacy-hardened: request/response dumps keep the JSON structure (model, ids, usage, role) but omit ALL business content — prompt/completion/tool-arguments/reasoning text under the known content keys is replaced with `<omitted>` (string arrays, text blocks and nested argument objects recursed in content mode), non-JSON or top-level-scalar bodies are replaced with a tiny placeholder plus the original byte length, and every dump is capped at 64 KiB with a truncation marker (a misbehaving upstream can no longer fill the disk). Model-cache fetch cancellation semantics fixed: the `/v1/models` request context now bounds ONLY the wait (`ModelCache.FetchWithContext`) — a disconnected client returns immediately while the leader's shared upstream round keeps running on the cache's own work context (concurrent requests and background fills still get the result), and a cancelled request answers nothing (never a 200 empty list, which a client would cache as "no models" forever, and never an unreadable error). `account.base_url` validation tightened: a query string (including a bare trailing `?`) or fragment is rejected at load — the upstream path is appended by a plain string join, so a query would corrupt every joined request URL and could carry `?key=secret` the operator never intended to send upstream; the error still never echoes the URL or query (they may embed credentials). Redaction depth cap unified: `util`'s duplicate local depth constant and its TODO are gone, `RedactJSON`/`RedactJSONWithKeys` now share the single `config.RedactJSONMaxDepth`. `prism quota` / `format=table` phone cards are indented two spaces on every line (title, errors and window rows) so stacked cards read as one aligned block.

- **2026-08-13** — v0.18.2 — fix: `prism quota` / `format=table` is one phone card per account (load-balanced plans are never merged). Window labels are short/mid/long. Columns pad by terminal cell width (CJK = 2); the rate-limited marker sits at the end of the line so percent and countdown stay aligned.

- **2026-08-13** — v0.18.1 — fix: `prism quota` / `format=table` is a stacked phone-width card (provider, accounts, then `5h`/`week`/`month` + percent + countdown). No header row, no ISO reset timestamps.

- **2026-08-13** — v0.18.0 — feat: upstream plan quota for OpenCode Go, separate from the local token usage ledger. New `quota` config (default on; `refresh_interval` 120s, `request_timeout` 5s) polls `GET {base}/usage` with the account key, de-duplicates by key fingerprint, and caches rolling/weekly/monthly `percent` + `resets_at` + `status`. Surfaces: `prism quota` (service-independent) and `GET /admin/quota` (same `PRISM_ADMIN_TOKEN` fail-closed auth as `/admin/usage/summary`; `format=table` shares the CLI renderer). JSON marks dollars as `limit_usd_estimate` / `usd_status=estimated` (documented $12 / $30 / $60 windows, floor percent). Poller is concurrent, Stop-cancellable and idempotent; fetch failures keep the last good snapshot as stale. Does not change account selection or `MarkExhausted`. Admin auth extracted to `internal/adminauth` and reused by usage summary.

- **2026-08-13** — v0.17.1 — audit round 7/8: 30 reliability/security fixes. `Account.Release` no longer leaks the account-wide total when a slot's per-key counter was abnormally zeroed: the total unit is returned via a non-negative CAS (return true → `Pool.Release` wakes a waiter; duplicate release still ignored), with regression tests for the zeroed-key, mixed-live-slot and zero-total cases plus a pool-level waiter-wake test. Client-IP resolution (`GetClientIP`) is stricter: `X-Real-IP` is accepted ONLY when `X-Forwarded-For` is absent AND the value is a valid IP outside the trusted CIDRs — an all-trusted XFF, a trusted/invalid X-Real-IP and every untrusted-RemoteAddr case fall back to RemoteAddr (a proxy-fed X-Real-IP is an independent client-spoofable claim and is never consulted while XFF is present); the trusted_proxies comments/README/example no longer claim 0.0.0.0/0 lets clients spoof XFF/X-Real-IP past the limiter (with every address trusted the chain walk collapses to RemoteAddr; per-client limiting keys on the proxy; X-Real-IP is only accepted when untrusted). Upstream redirects are host-bounded: `newHTTPClient` follows same-host redirects but refuses cross-host hops outright (Go only strips the standard sensitive headers on a cross-host hop — a custom `auth_header`/account headers would leak to the redirect target). The usage `default_key_id` fallback is an `atomic.Value` (SIGHUP reload writes while request goroutines read — no more data race). Ollama-host detection uses the URL hostname, case-normalized, accepting only `ollama.com` and its subdomains (`evilollama.com` no longer qualifies). Account names are validated at load (non-empty, unique, ASCII alnum start, charset `[A-Za-z0-9_-]`, ≤64 bytes — no dots/path separators/spaces — and no two names may fold to the same `PRISM_KEY_*` credential, e.g. `a-b` vs `a_b`); `prism setup` now rejects exactly what LoadConfig rejects GLOBALLY — exact duplicates and `CredentialEnvName` fold collisions (`a-b` vs `a_b` → `PRISM_KEY_A_B`) across providers — via the new public `config.CredentialEnvName` shared by LoadConfig and setup (setup's credstore/unit generation reuse it too), refuses to generate a config for a non-loopback listen (shared `config.IsLoopbackListen`: `localhost` any-case counts as loopback; empty host / `0.0.0.0` / `::` do not), and validates `host:port` explicitly so a bare `127.0.0.1` gets a clear "missing port" error instead of a wrong "not loopback" claim. The MCP tool cache caps a SINGLE tool's serialized size (1 MiB; oversized tools are dropped — never truncated, never logged in full) on top of the per-identity 100-tool cap, and `json.Marshal` + the 1 MiB size check moved OUT of the global cache lock (the lock now covers only the final dedupe/cap/append), with a concurrent-writes test. `HasForwardedHeaders` now also recognizes the RFC 7239 `Forwarded` header (`/metrics` and `/admin/usage/summary` token-free loopback is denied when it is present). Upstream requests never forward `Host`, `Content-Length` or `Expect`. Model-cache provider files are 0600 (owner-only) on the fetch-rename AND the load path (`LoadFromDisk`, pi `models.json` untouched), and the usage SQLite database plus its `-wal`/`-shm` sidecars are tightened to 0600 after a writable open, after migrate and after every successful write transaction (`InsertBatch`/`DeleteBefore`): the data transaction is already committed and the functions keep returning success, so a permission-tightening failure is reported via an error-level log plus the `usage_write_errors` counter — never as a returned error or rollback (an error return would make the writer retry a committed batch and the retention loop re-report a committed delete) — verified by runtime-recreation and wide-umask tests. Stale `probe_model` documentation removed from the README and `config.yaml.example`. The rate limiter's bucket cap no longer evicts under the lock: at the cap a NEW IP is rejected outright (no O(n) scan; existing buckets untouched). New `/ready` endpoint: 200 `ok` only when at least one account is healthy and out of cooldown, else 503 `not ready`; `/health` stays pure liveness; both bypass the api_keys gate and the rate limiter. `/ready` deployment semantics are fail-closed: all-accounts cooldown/exhausted fails the deploy health check and rolls back by design; `HEALTH_URL=/health` is the explicit liveness-only override; the docs warn against blindly stretching `HEALTH_TIMEOUT` to minutes. `deploy.sh`/`deploy_health.sh` default `HEALTH_URL` to `/ready` (`HEALTH_URL` override kept); `/ready` 503s are logged at DEBUG (deploy scripts poll every second — no warn flood). `pool.Ready`'s cooldown boundary now matches `Select` (`!now.Before(cooldownUntil)` — `now == cooldownUntil` counts as expired) with a boundary test. The usage summary admin token (`PRISM_ADMIN_TOKEN`) is re-read from the environment on every request (rotation takes effect without a restart, like `/metrics`). `scripts/install.sh` builds `./cmd/prism` (temp artifact in `/tmp`). `codex-wrapper.sh` never execs itself: it resolves the real codex through PATH (skipping its own symlink) and refuses an EXPLICIT `CODEX_BIN` that resolves (via `readlink -f`) to the wrapper itself or its symlink — exit 127 with a clear message, no recursion (script tests added). The `pre-commit` hook handles spaced/quoted file names (NUL-delimited), scans only added lines, and drops the false-positive-prone long-hex/base64 rules (keeps `sk-*`, AWS `AKIA`, GitHub PATs, Google OAuth, JWT, private-key blocks). `generate_mcp_tools.py` parses TOML with the stdlib `tomllib` (legal complex TOML supported; malformed TOML fails fast with a non-zero exit). New/updated script tests: `test_codex_wrapper.sh`, `test_pre_commit.sh`, `test_generate_mcp_tools.py` (plus `test_deploy_health.sh` unchanged); `test_pre_commit.sh` runs every direct hook invocation inside the temp repo with `GIT_DIR`/`GIT_INDEX_FILE`/`GIT_WORK_TREE` cleared, so it never reads the caller's staged content.

- **2026-08-12** — v0.17.0 — audit round 6: 8 reliability fixes. Per-model concurrency is keyed by the MODEL NAME (the same string `max_concurrent_per_account` looks up), never by the max VALUE: each model owns an independent in-flight counter (two models with the same max no longer share a counter and can be limited independently), and a new explicit `max_concurrent_per_account_total` caps the account-wide aggregate so N configured tiers can never stack N×max in-flight (absent → backward-compatible default = sum of distinct positive per-model values, the old worst case; restart-required on change, ReloadConfig warns). `Acquire`/`Release` now pair through a non-mismatchable lease: `TryAcquire` returns a `*Slot` carrying the exact account/key/max and `Release` takes only that slot — a caller can no longer pass a wrong max and corrupt the accounting; waiter capacity checks and wakeups apply the identical per-key + total gate. Usage recorder loss is never silent: every drop (queue full / recorder not started or closed / failed batch / per-event insert failure) is counted in `/metrics` (`usage_events_dropped`, `usage_write_errors`) AND logged with a rate-limited warning (5s interval, no log flood), and a new `usage_recorder_status` expvar reports the recorder lifecycle; docs now state explicitly that usage recording is best-effort and NOT a strict billing source. Config loading fails fast for provider-less accounts (**compat note**: bare top-level `accounts:` entries must now declare `provider:` or live under a `providers:` block — they could never be selected by business requests, so they only hid a dead account) and for whole-address-space `trusted_proxies` (`0.0.0.0/0`, `::/0` — they would let any client spoof the forwarding headers past the IP rate limiter); both validations also run on the reload path. `util.DebugMode` is an `atomic.Bool` (SIGHUP reload writes it while request goroutines read it — the old plain bool was a data race). Legacy stream usage capture is now SSE-EVENT-AWARE and bounded: CRLF streams are normalized, the final event without a terminating empty line is flushed at EOF, events are split correctly under arbitrary chunk cutting, over-cap events are skipped without disturbing the events around them, and memory stays bounded (4 MiB event cap, 16 KiB head, 64 KiB recent) — huge content events can no longer evict the usage carriers (OpenAI and Anthropic regression tests). `TestReadOnlyConcurrentWithLiveWriter` is deterministic: the writer produces a fixed event count and the reader waits for provable overlap instead of a fixed 2-second window. A client disconnect before the upstream connect is audited as status 499 / `error_type=client_disconnect` (only when the request context is really canceled) instead of a bare status 0.

- **2026-08-12** — v0.16.1 — audit round 5: 12 reliability/security fixes. `/v1/responses` streaming translation bounds its accumulation buffers (output text, reasoning text, and each tool call's arguments, 16 MiB per buffer) AND the total across all of them (32 MiB: text + reasoning + every tool call's arguments combined, so N tools can no longer hold N×16 MiB; the counter is overflow-safe): an upstream that streams without bound aborts with an explicit `response.failed` (`response_too_large`) when the 200 is already committed, or a pre-first-event HTTP 502 with the same `response_too_large` code (never silent truncation, never a generic `upstream_stream_error`). The Responses SSE parser accepts both spec forms of the data field (`data: <payload>` and `data:<payload>`). `probe_path: disabled` no longer optimistically revives exhausted accounts ("probing disabled" is not "the credential recovered"): disabled accounts are left untouched and stay exhausted until the operator restores them (startup check already behaved this way; the probe loop now matches). `METRICS_TOKEN` / `PRISM_ADMIN_TOKEN` longer than 256 bytes fail startup explicitly (the constant-time pad can never match them; the error never echoes the token). `X-Request-ID` is validated: over 128 bytes or containing control characters / invalid UTF-8 is rejected with HTTP 400 `invalid_request_id` (the ID is echoed into the response header, audit log and usage database — no injection). The built-in per-account concurrency default is conservative (8) and no longer guesses a DeepSeek tier from arbitrary model-name substrings (`flash`/`pro`); the operator's `max_concurrent_per_account` config is the only way to raise it (public resolution functions unchanged). SIGHUP reload no longer publishes a config whose accounts differ from the running pool: account changes keep the RUNNING accounts (with a clear warning; provider→effort schema and `default_provider` stay consistent with them) while every other field still hot-reloads. Non-loopback listeners now REQUIRE complete TLS (both `tls_cert_file`/`tls_key_file` or their `PRISM_TLS_CERT`/`PRISM_TLS_KEY` env fallbacks): loading fails without it unless `allow_insecure_http: true` explicitly opts into plaintext (a warning is still logged), configuring exactly ONE of cert/key is always a load error, `trusted_proxies` no longer counts as safety (it cannot prevent direct access to the listener), and loopback listeners stay TLS-free for development. The stream scanner's 4 MiB single-line limit is enforced explicitly in the split function (the default ScanLines check is bypassed when the initial buffer exceeds the cap): an over-limit SSE line yields a diagnosable `stream_line_too_long` frame mid-stream or the same code on a pre-first-event HTTP 502 instead of a generic stream failure. `account.base_url` is validated at load time (absolute http(s) URL with non-empty host; a config-correctness check — base_url is operator-controlled, deliberately not framed as SSRF protection; errors never echo the URL, which may embed credentials). `api_keys` names equal to the reserved unauthenticated MCP identity (`unauthenticated`) are rejected at load, so an authenticated key can never collide with — or shadow — the read-only unauthenticated tool-cache bucket. Debug logs no longer record content/reasoning/refusal payloads — only safe metadata (lengths, counts, types). With auth disabled the MCP tool cache uses a fixed read-only unauthenticated identity: request-path writes are refused and unauthenticated requests see only the admin-injected bucket, so different local clients can no longer pollute each other's cached tools.

- **2026-08-12** — v0.16.0 — audit round 4: 14 reliability/security fixes. Non-streaming upstream 2xx body-read failures no longer return an empty 200: while the response is uncommitted the proxy answers a structured 502 `upstream_error` and audits 502 (responses and legacy chat paths). MCP tool cache is isolated per API key identity: the cache key is the authenticated key NAME from the auth middleware (stable, non-secret) instead of the fixed `tenant=default` — tools cached from one key's namespace bundles are never visible to another key, while the admin-injected `mcp_tools.json` bucket stays visible to every identity (own tools win on name collision). With usage recording enabled, OpenAI-compatible `stream=true` requests get `stream_options.include_usage=true` injected (other client `stream_options` fields preserved; the Anthropic /v1/messages surface is never touched). Client `RawQuery` is no longer forwarded to the upstream (the upstream URL is built from the account base URL + fixed path only). `SimplifyJSONSchema` is depth-bounded (32 levels, `SimplifyJSONSchemaLimited` returns `ErrSchemaTooDeep`): a too-deep client tool schema fails the request with a clear 400 instead of recursing unbounded or falling back to the unsafe original. All-accounts upstream 401/402 exhaustion no longer masquerades as a generic 503: the terminal response distinguishes upstream credential failure (502 `upstream_auth_failed`) from quota/balance exhaustion (503 `upstream_quota_exhausted`; 402 is now classified as a money failure, not a credential one); failover across accounts is unchanged. `/health` bypasses the business rate limiter (liveness must stay reachable under load) and no other path is exempted. Streaming /v1/responses delays the HTTP header until the first event: a failure before the first event returns a structured 502 `upstream_stream_error`; a mid-stream failure after the 200 is committed delivers the protocol `response.failed` terminal event and the audit keeps the committed 200 (the HTTP status cannot change after commit). Model remap traceability: the audit records the resolved `upstream_model` next to the requested model, and pricing prefers the actual upstream model's price with a fallback to the virtual model's (no schema migration). Model-cache provider file names are validated at config load (path separators, `..`, absolute paths rejected) and defensively at the write path. `max_concurrent_per_account` values above int32 are rejected at load (they would truncate in the pool's int32 concurrency accounting). `Retry-After` now honors both delta-seconds and HTTP-date (past dates → default cooldown, huge values capped, no duration overflow). The usage/audit gate accepts cache-only usage (e.g. Anthropic `cache_read_input_tokens` with zero input/output) at every capture point. `StatusCapture.WriteHeader` accepts only the first call — later calls are no-ops and never reach the underlying writer, matching net/http semantics.

- **2026-08-12** — v0.15.0 — audit round 3: 10 reliability/security fixes. Model cache fetch: multi-account failover across all healthy non-cooldown accounts (saturated → next account; network/5xx/auth/parse failure → next account; one shared 30s budget for the whole failover, never N×30s; all accounts unhealthy → `ErrNoHealthyAccount`, all saturated → `ErrFetchSaturated`, otherwise the last safe error; every acquired slot released exactly once via the pool). Same-provider fetches are merged: a hand-written per-provider inflight map keeps at most one leader per provider (all Fetch/refresh/cache-miss callers share the leader's result/error; a follower can cancel its own context without cancelling the leader; different providers stay parallel; the merge lock never spans the wait). `RefreshAll` no longer bypasses the scheduler: it runs as a synchronous manual round (refused while another round is in flight or after Stop, cancellable and waited by Stop). `/v1/models` fetch connection errors and `api/show` errors are classified (`upstream_timeout`/`upstream_refused`/…) instead of embedding the raw `*url.Error` (which carries the full URL + query); startup checks, probe-loop logs and upstream chat connection logs likewise log only `error_type` and safe fields — a `?key=secret` URL can never reach a log. `setup`'s generated systemd unit now matches `scripts/prism.service.example` security hardening (NoNewPrivileges, ProtectKernelTunables/Modules/ControlGroups, LockPersonality, RestrictSUIDSGID/Namespaces, SystemCallArchitectures, AmbientCapabilities, RestrictAddressFamilies, TimeoutStopSec, KillMode, ReadOnlyPaths) and `ReadWritePaths` covers the real model cache dir, the usage.db parent dir and tool config dirs (relative paths resolved against WorkingDirectory, `Clean`ed, deduplicated; example synced; pure-function tests + `systemd-analyze verify` of the generated text). `SyncTools` aborts with an error when the temp-file chown cannot preserve the existing uid/gid — the old in-place fallback is gone, the deployed file stays byte-for-byte intact and no temp litter remains. `ReloadConfig` no longer warns that `debug` needs a restart (it is hot-reloadable). `/v1/responses` early rejections (body read / conversion) now emit exactly one `request.complete` audit with the correct status/error_type/model/request_id — shared `readRequestBody` audits for all three POST surfaces, and the success path still audits exactly once. `copyClientHeaders` drops `Forwarded`, all `X-Forwarded-*` and `X-Real-IP` (client-spoofable forwarding headers) while normal business headers still pass through. `StatusCapture` implements `Write`: an implicit write records 200 before the first byte, an explicit status is never overwritten.

- **2026-08-12** — v0.14.1 — audit round 2: 10 reliability/security fixes. Model cache: `Fetch` releases its concurrency slot through the pool (`mc.pool.Release`), so a completed fetch wakes a parked provider waiter (previously waiters could strand until an unrelated request freed the account); `/v1/models` and ollama `/api/show` SUCCESS responses are now read with the same bounded helper as chat responses (`max_upstream_response_bytes`, default 32 MiB, invalid caps never degrade to an unbounded read — helper moved to `internal/util` so cache and proxy share it without an import cycle); SIGHUP no longer blocks the signal loop on `RefreshAll`/`SyncTools` — a controlled background refresh (reentry-coalesced, internal cancellable context, `Stop()` aborts an in-flight refresh and waits for it, no cache file written after cancellation, config snapshotted to avoid races with `UpdateConfig`) runs the refresh + tools sync; `/v1/models` cache-miss fetch failures are classified instead of returning 200 empty lists — no healthy account → 503 `no_healthy`, saturated → 503 `model_fetch_saturated`, other upstream/parse/size errors → 502 `model_fetch_failed`. Upstream error handling: every 401/402/429/4xx/5xx log and audit error body (runtime, startup probe, probe loop) is redacted with `RedactBodyBytesWithKeys(..., acc.Key())`, closing the leak for account keys that are not `sk-`/Bearer shaped (custom `auth_header`). Request bodies: `/v1/chat/completions`, `/v1/responses`, `/v1/messages` bodies over 10 MiB return HTTP 413 `request_too_large` (JSON envelope), other read errors return HTTP 400 `invalid_request` — one shared helper instead of three divergent paths. Auth: the Bearer scheme is matched case-insensitively (`bearer`/`BEARER`/`BeArEr`) across `Authenticate`, `CheckAuth` and the usage admin auth, while the token bytes stay strictly compared (bare tokens and wrong tokens still rejected). Anthropic SSE wire-format detection parses the `data:` JSON `type` field (whitespace- and field-order-insensitive) instead of matching the exact byte string `"type":"message_start"`. `deploy.sh` health-check URL is now overridable via the `HEALTH_URL` environment variable (default unchanged). All round-1 guarantees kept: empty-token keys, fail-closed admin auth, cross-provider waiters, XFF multi-hop, 256 MiB cap, select error codes, cost clamping, bucket cap, 405.

- **2026-08-12** — v0.14.0 — audit round: 14 reliability/security fixes. Account selection: select timeout now bound to `r.Context()` (client disconnect cancels the wait immediately) and select failures are classified with `errors.Is` into four response codes — `no_healthy` / `select_timeout` / `client_canceled` / `select_failed` — replacing the old single `no_accounts` code on the select path (HTTP stays 503; **compat note**: clients matching on `code` must update). Pool waiters are provider-aware: `Release`/`MarkHealthy` wake the first queued waiter that can use the freed slot (provider match, or `provider=""` waiters that can use any slot), FIFO within the matching set, no lost wakeups. Auth hardening: `api_keys` with empty/whitespace tokens are rejected at load and skipped defensively by `Authenticate`; `/metrics` and `/admin/usage/summary` auth is fail-closed — when `METRICS_TOKEN`/`PRISM_ADMIN_TOKEN` is configured EVERY request (loopback included) must present it as a Bearer token, and token-free direct loopback (no `X-Forwarded-For`/`X-Real-IP`) is allowed only when the token is unset. Upstream response handling: non-streaming success reads capped at `max_upstream_response_bytes` (default 32 MiB, hard upper bound 256 MiB, read max+1, over-limit → 502 `response_too_large`); `IsQuotaError` now accepts only the structured permanent quota envelope (`insufficient_quota`/`gousagelimiterror`) — plain-text quota messages on 429 go to cooldown; bare 403 is never permanent, while a 403 carrying a recognized structured credential/quota body exhausts the account via the shared `ClassifyUpstreamError` used by both runtime and startup — the original 403 body/status still passes through to the client (body read once, redacted). Trusted-proxy client IP: `GetClientIP` walks XFF right-to-left skipping all trusted hops (multi-hop chains resolve to the real client). Model cache: non-200 error bodies are redacted before entering errors/logs; `Fetch` now `TryAcquire`s a concurrency slot and fails fast when saturated, using `config.ResolveFetchConcurrency` (configured `*` wildcard, else the smallest positive per-model limit, else the built-in default) because a fetch is not tied to a single business model. `/v1/chat/completions` and `/v1/responses` return 405 `method_not_allowed` for non-POST. Rate limiter buckets have a deterministic cap (default 100000; oldest `lastCheck` evicted; test-injectable). Cost accounting: all four token counters (prompt/completion/cached/cache-write) are clamped non-negative at the cost entry and at every parse entry — OpenAI keeps cached ≤ prompt, Anthropic only drops negatives and keeps its formula semantics — so no broken upstream report can ever produce a negative cost.

- **2026-08-11** — v0.13.2 — fix: `prism usage` summary line now splits the cache-hit figure into two independent segments by `usage_source` (OpenAI vs Anthropic), each computed with its own basis: the OpenAI segment is `cached/prompt_tokens` (OpenAI `prompt_tokens` includes cached tokens), the Anthropic segment uses `cache_read/(input_tokens+cache_read+cache_creation)` (Anthropic `input_tokens` excludes the cache counters, `cache_read` is a sibling field) and can never exceed 100%; previously the two wire formats were mixed into one ratio with numerator and denominator from different bases, which produced hit rates over 100% whenever Anthropic `cache_read` dwarfed its tiny `input_tokens` (e.g. the claude-opus-5 batch). `usage_source` values `'openai'`, empty string and NULL all bucket into the OpenAI segment, matching the pricing partition in cost.go; a segment with no rows is omitted from the summary, and the per-row detail table keeps its original per-row cache column

- **2026-08-11** — v0.13.1 — fix: `prism usage` CLI config lookup now walks a fallback chain (`--db` > `config.yaml` in the current directory > `/var/lib/prism/config.yaml` > built-in default), so running the command outside `/var/lib/prism` no longer silently falls back to the wrong default path `/var/lib/prism/usage.db` while the real database lives at `/var/lib/prism/usage/usage.db`; the missing-database error no longer asserts "usage not enabled" but reports the actual path, where it came from, and a `--db` hint

- **2026-08-08** — v0.13.0 — feat: token usage recording (internal/usage SQLite store + batched recorder) wired into the request lifecycle and config: new `usage` config section (opt-in, default disabled; `db_path` not hot-reloadable), per-request usage/cost persisted at `EmitAudit` via an injected minimal recorder interface (middleware stays decoupled from internal/usage), cost priced per request from `model_metadata[].cost` (USD per 1M tokens, no conversion), admin endpoint `GET /admin/usage/summary` (PRISM_ADMIN_TOKEN Bearer auth or localhost, mounted before the global api_keys gate). Hard degradation guarantee: every usage failure (store open/migrate, disk full, SQLITE_BUSY, full queue, pricing) is logged + counted only, never returned, never panics, never blocks request finalization or /v1 forwarding. Also tightened `Authenticate` to require the `Bearer ` prefix (bare tokens are now rejected, matching legacy `CheckAuth`)

- **2026-08-21** — v0.21.1 — fix: new_api_error-family relays (e.g. agentrouter) report an empty balance as a bare 403 with `error.code` `insufficient_user_quota`; the classifier now recognizes that envelope as a permanent quota error, so the account is exhausted and the request fails over to the next account instead of passing the 403 straight to the client every time round-robin lands on it; unrelated relay error codes still pass through untouched (strict exact-code matching, no substring fallback)

- **2026-08-08** — v0.12.0 — feat: new `default_provider` config option; requests missing the X-Prism-Provider header now route through the configured default provider when set, otherwise return HTTP 400 (missing X-Prism-Provider header) instead of falling back to whole-pool account selection, which could previously hit an account of a different provider (e.g. deepseek-v4-flash landing on agentrouter-ant-2, gpt-5.6-sol landing on agentrouter-ant-1); explicit `max_concurrent_per_account` example added so per-model concurrency is configured instead of falling back to the built-in default with an "unknown model" warning

- **2026-08-08** — v0.11.0 — fix: account selection is now true per-provider round-robin. Previously all providers shared a single pool-wide cursor whose start index was computed modulo the total number of accounts, and the cursor advanced by the number of accounts scanned per attempt; a provider's first account on the ring was therefore picked disproportionately often (measured 3:0 on one two-account provider and 6:2 on another), and high-traffic providers polluted the rotation of low-traffic ones. Each provider now has its own cursor that rotates strictly within its own account subset, advancing exactly one position per successful selection, and the full-pool Select path advances its cursor the same way; new tests guard rotation order, cross-provider isolation, cooldown skipping, and uniform full-pool rotation
- **2026-08-08** — v0.10.2 — docs: correct the syncPIModelsJSON write-strategy comment — direct overwrite preserves the existing owner/mode of pi models.json, which is what lets prism write it through group permissions; tmp+rename would reassign ownership
- **2026-08-08** — v0.10.1 — fix: per-account headers and auth_header now also apply to model cache fetches (/v1/models and ollama /api/show), previously only chat forwarding and probes carried them — gateways that authenticate on client identity headers rejected model fetches with 401; skip_pi_sync narrowed to its original meaning (never overwrite hand-maintained pi models.json entries) and no longer suppresses upstream model fetching
- **2026-08-08** — v0.10.0 — feat: agentrouter-class external provider accounts join the pool load balancer; per-account headers and auth_header (default Authorization Bearer, configurable e.g. x-api-key); anthropic /v1/messages pure passthrough route independent of wire_api gating; per-account probe_path with disabled support (optimistic recovery, no permanent exhausted when probe endpoint unavailable); skip_pi_sync opt-out so hand-maintained pi models.json entries are never overwritten
- **2026-07-29** — v0.9.3 — feat: add grok-4.5 reasoning profile (FormEnum, reasoning_effort low/medium/high)
- **2026-07-27** — v0.9.2 — fix: TransformRequestBodyForProvider normalizes `role:developer` → `role:system` in /v1/chat/completions, gated to ollama-schema providers only (Ollama silently drops developer-role content, causing SYSTEM.md/AGENTS.md loss for reasoning models with auto-detected supportsDeveloperRole=true; opencode-schema providers unaffected)
- **2026-07-27** — v0.9.1 — fix: TransformRequestBodyForProvider now normalizes `role:developer` → `role:system` in /v1/chat/completions messages (Ollama silently drops developer-role content, causing SYSTEM.md/AGENTS.md to be lost for reasoning models auto-detected as supportsDeveloperRole=true)
- **2026-07-27** — v0.9.0 — feat: startup auto-fetch ollama /api/show (self-heal old cache without Meta, no manual SIGHUP) + provider-scoped model_metadata (default + per-provider override layer, same model no crosstalk across providers, e.g. deepseek-v4-pro ollama upstream 512K vs opencode-go config 1M)
- **2026-07-27** — v0.8.0 — feat: model metadata single source of truth — fetch from upstream (ollama /api/show for context_length) + config model_metadata field-level override, syncPIModelsJSON rewrites existing models too (fixes glm-5.2 128k→1M), /v1/models returns upstream+config merged metadata, cache ModelMeta + providerCache.Meta
- **2026-07-27** — v0.7.0 — feat: provider-dimension effort mapping (X-Prism-Provider header → opencode/ollama schema), same model no crosstalk across providers (glm-5.2/deepseek differ opencode vs ollama), ollama profiles match ollama.com real levels (xhigh→max, off→none, no thinking.type), drop outer ModelRemapEnabled gate (Apply always runs, remap still internally gated), config base_url host detection for provider schema
- **2026-07-27** — v0.6.2 — feat: generic reasoning effort mapping (internal/reasoning package), downward-proximity clamp, per-model effort remap for non-DeepSeek models (hy3/glm/kimi/qwen/mimo/minimax), DeepSeek behavior unchanged
- **2026-07-25** — v0.6.1 — fix: gofmt alignment, Non-Prim→Non-Prism typo, syncPIModelsJSON unmarshal-fail pc reset, atomic write (tmp+rename)
- **2026-07-23** — v0.6.0 — /v1/models returns model_metadata (context_window/max_tokens/cost/think etc.), syncPIModelsJSON changed to merge (no overwrite), config adds model_metadata section
- **2026-07-23** — v0.5.2 — provider routing (X-Prism-Provider), disk model cache, prism setup command, probe changed to GET /v1/models, model_remap_enabled toggle, credential three-tier fallback, cleanup of ProbeModel dead code
